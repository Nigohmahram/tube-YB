import { Link } from 'react-router-dom';
import { Button } from '@mui/material';

const Main = () => {
	return (
		<Link to={'/chanel'}>
			<Button>button</Button>
		</Link>
	);
};

export default Main;
